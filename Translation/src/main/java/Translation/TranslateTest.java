package Translation;

import static com.codeborne.selenide.Selenide.$x;
import static com.codeborne.selenide.Selenide.open;

import com.codeborne.selenide.Configuration;

import io.github.bonigarcia.wdm.ChromeDriverManager;
import io.github.bonigarcia.wdm.DriverManagerType;

public class TranslateTest {

	/*
	 * public static void main(String args[]) {
	 * 
	 * try { ChromeDriverManager.getInstance(DriverManagerType.CHROME).version(
	 * "76.0.3809.126").setup(); Configuration.startMaximized = true;
	 * open("https://google.com"); String[] strings = { "King" }; for (String data :
	 * strings) { $x("//textarea[@id='source']").clear();
	 * $x("//textarea[@id='source']").sendKeys(data); String translation =
	 * $x("//span[@class='tlid-translation translation']").getText();
	 * System.out.println(translation); } } catch (Exception e) {
	 * e.printStackTrace(); }
	 * 
	 * }
	 */
	
	
	
	
}
